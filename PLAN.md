# 个人能力训练打卡网站计划

## A. 产品一句话定位与目标用户

一句话定位：一个为“刚毕业、在英国找工作、英语非母语”的使用者设计的每日能力训练账本，把输入、思考、表达、整理、职场沟通和复盘固定成可持续的 12 周打卡习惯。

目标用户：

- 刚毕业或即将毕业，正在英国求职，需要持续提升英文表达、结构化思考和职场沟通能力的人。
- 中文为主要思考语言，但需要每天产出一小段英文练习内容的人。
- 不需要复杂社交和排行榜，只需要一个安静、可信、每天愿意打开的个人训练工具的人。

## B. 信息架构

整体结构：单个 `index.html` 内完成五个页面，通过顶部学术编号导航切换视图；URL 使用 hash，例如 `#today`、`#week`、`#plan`、`#templates`、`#progress`，双击本地文件也能工作。HTML 明确使用 `<html lang="zh">`，并补充 `<title>`、`meta description` 和内联 favicon。

### §1 今天

内容：

- 日期、星期、按“第 1 周开始日期”推算的当前周次。
- 本周主题、本周目标、今天该做什么，来自 12 周计划。
- 六个训练环节：Input、Think、Output、Organize、Comms、Review。
- 今日笔记文本框。
- 一句反思文本框。
- 小统计：今日完成 `X/6`、当前连续达标天数、今日是否达标。

交互：

- 六个训练环节以“账本条目”呈现，使用方形 ballot box 和细线边框。点击或键盘回车/空格切换完成状态。
- 完成状态、今日笔记、一句反思即时保存。
- 完成数达到 4 段即显示“达标”，否则显示“未达标”。
- 切换页面再回来、刷新页面后仍保留今天数据。
- 若当前真实周次小于 1，今天页显示“未开始”，本周主题兜底为“计划尚未开始”，今日任务兜底为“先确认第 1 周开始日期，或提前做一次轻量输入与复盘”。

### §2 本周

内容：

- 周选择器：上一周、当前选择周、下一周，默认显示当前周。
- 七天 × 六环节只读表格，行是日期与星期，列是训练环节。
- 每个单元格用印刷方块表示该日该环节是否完成。
- 末列显示当天“一句反思”的短摘要。
- 本周达标天数 `X/7`。
- 本周复盘检查项，取自对应周计划的复盘方式。

交互：

- 前后翻周，当前真实周次用瓶绿细线和 small caps 标记。
- 表格只读，所有数据来自“今天”页保存的 `day:YYYY-MM-DD` 记录。
- 反思摘要过长时截断，完整内容放在 `title` 提示里。

### §3 计划

内容：

- 完整 12 周计划，按三阶段展示：
  - 阶段一 W1-4：打地基。
  - 阶段二 W5-8：上强度。
  - 阶段三 W9-12：整合 + 求职落地。
- 每周显示：周次、标题、目标、每日任务、复盘方式。

交互：

- 当前周高亮。
- 计划以学术目录和条目式排版展示，不做折叠，方便快速扫读。
- 若当前日期超出 12 周范围，仍保留最近有效计划高亮逻辑，并在页面上以低调文字提示“计划周期外”。

### §4 模板

内容：

- 六个可复制训练模板，原样使用需求中的内容。
- 表单型模板用“字段：______”呈现。
- 英文骨架模板使用等宽字体块，空位保留 `___` 占位符。

交互：

- 每个模板提供“复制”按钮。
- 复制成功后按钮文字短暂改为“已复制”，然后恢复。
- 复制失败时选中文本区域，方便手动复制，不弹出刺眼提示。

### §5 进度

内容：

- 统计卡：累计达标天数、当前连续、最长连续、已开始 N 天的达标率。
- 最近 12 周热力图：12 列 × 7 行，每格一天，以今天所在日期为结尾窗口，保证今天格始终存在。
- 今天格高亮。
- 最近若干条反思记录，带日期。
- 设置区：
  - 设置“第 1 周开始日期”。
  - 导出全部数据为 JSON。
  - 从 JSON 导入。
  - 清空全部，需要二次确认。

交互：

- 热力图颜色深浅按当天完成环节数 `0-6` 显示，使用同一瓶绿的不同不透明度和纸色底，不引入额外强调色。
- 开始日期变更后，今天页、本周页、计划页和热力图全部重新计算。
- 导出生成包含 `version`、`exportedAt`、`meta`、`days` 的 JSON 文件。
- 导入时校验 JSON 结构，采用按天合并：逐个 `day:` 键写入，冲突以导入文件为准；`meta` 若存在合法开始日期则更新，不整体替换其它应用外数据。
- 清空全部使用二次确认：第一次点显示确认状态，第二次才真正清空。

## C. 数据模型

存储策略：

- 首选 `localStorage`。
- 所有读写操作包裹 `try/catch`。
- 如果 `localStorage` 不可用，退回内存对象 `memoryStore`，页面仍能在当前会话中使用，但刷新后不保证保存。

键设计：

- `ability-trainer:v1:meta`
- `ability-trainer:v1:day:YYYY-MM-DD`

`meta` 结构：

```json
{
  "start": "2026-06-20"
}
```

每日记录结构：

```json
{
  "seg": {
    "input": false,
    "think": false,
    "output": false,
    "organize": false,
    "comms": false,
    "review": false
  },
  "note": "",
  "reflection": "",
  "updatedAt": "2026-06-20T10:30:00.000Z"
}
```

导出结构：

```json
{
  "version": 1,
  "exportedAt": "2026-06-20T10:30:00.000Z",
  "meta": {
    "start": "2026-06-20"
  },
  "days": {
    "2026-06-20": {
      "seg": {},
      "note": "",
      "reflection": ""
    }
  }
}
```

核心计算：

- 日期 key：使用浏览器本地日期生成 `YYYY-MM-DD`，避免 UTC 跨日问题。
- 当前第几周：`floor((today - startDate) / 7) + 1`。
- 未开始边界：若当前真实周次 `< 1`，今天页显示“未开始”，计划信息使用兜底文案，不访问不存在的周计划。
- 计划周次：显示时限制在 `1-12`；若真实周次小于 1 或大于 12，给出周期外提示。
- 达标：单日完成环节数 `>= 4`。
- 当前连续达标天数：如果今天达标，从今天往前数；如果今天未达标，从昨天往前数。
- 最长连续：从开始日期到今天扫描所有 day 记录。
- 达标率：从开始日期到今天的天数为分母，并使用 `max(1, daysElapsed)` 防止除零或负分母。
- 热力图：滚动显示最近 12 周，即以今天为窗口末尾向前连续 84 天；今天格始终存在并高亮。

启动流程：

- 初始 HTML 显示极简 loading：一行 small caps “Loading ledger...”
- JS 初始化存储。
- 若无 `meta.start`，默认写入今天。
- 载入每日数据并渲染当前 hash 对应页面。
- 数据加载完成后移除 loading，显示应用。

## D. 设计系统

设计关键词：极简复古学术风、旧大学出版社、学术期刊、卡片目录、实验室笔记本。排版是主角，界面克制而有秩序。

### 色彩

全站只使用这些固定色值，并严格区分文字、线条与强调用途：

- 纸张底色：`#F1EEE4`，偏冷象牙纸，不使用纯白。
- 正文墨色：`#25211B`，暖调近黑。
- 次级文字：`#5E574B`，用于页边注、说明、日期、辅助标签等所有正文外文字，保证常规字号下对比度不低于 4.5:1。
- 发丝线：`#9B9386`，只用于边框、分隔线和表格线，不承载正文或说明文字。
- 唯一强调色：`#174D3A`，瓶绿。

选择瓶绿的理由：

- 瓶绿来自旧图书馆书脊、学术期刊封面和实验室账册，比赤陶色更冷静。
- 它和求职训练的“可靠、耐心、长期主义”气质一致。
- 用在当前状态、完成状态、今日高亮和少量交互反馈上，不大面积铺色。

不用的东西：

- 不用纯白、霓虹、渐变、毛玻璃、阴影。
- 不用大面积奶油色加赤陶色的 AI 默认组合。
- 不用现代 SaaS 风的大圆角卡片堆叠。

### 字体

- 主字体：`Newsreader`，通过 Google Fonts 引入，回退为 `Georgia, 'Times New Roman', serif`。
- 数据与英文模板：`IBM Plex Mono`，通过 Google Fonts 引入，回退为 `Consolas, 'Courier New', monospace`。
- 全站启用 old-style 数字：`font-variant-numeric: oldstyle-nums tabular-nums`。
- 标签、小标题、导航使用 small caps：`font-variant-caps: small-caps`。

### 字号层级

- 页面主标题：`clamp(2.2rem, 6vw, 4.8rem)`，用于扉页式标题区。
- 二级标题：`1.45rem`。
- 小节标题：`1.05rem`，small caps。
- 正文：`1.05rem`，行高 `1.65`。
- 辅助文字：`0.88rem`。
- 等宽数据：`0.9rem`。

### 间距与版心

- 最大版心：`1120px`。
- 正文行宽：`68ch` 左右。
- 桌面布局：正文 + 页边注的不对称两栏，主栏约 `minmax(0, 1fr)`，侧栏约 `220px`。
- 手机布局：单栏，导航可横向滚动。
- 基础间距：`4px` 网格，常用 `8 / 12 / 16 / 24 / 32 / 48 / 72px`。
- 页面顶部保留明显留白，像翻开一本书的章节首页。

### 关键组件样式

- 导航：学术编号式标签，例如 `§1 今天`，细线底边，当前页使用瓶绿文字与上划线。
- 训练条目：不是现代卡片，而是“账本格”；矩形细线、无阴影、轻微纸张底色差异；左侧方形 ballot box，完成后显示墨色或瓶绿的 `X`。
- 按钮：细线边框、无填充或极浅纸色填充，small caps 标签，焦点态用瓶绿 outline。
- 文本框：像实验记录页，细线边框、纸色底、衬线正文，focus 使用瓶绿下划线。
- 表格：有线账本风格，细线分隔，表头 small caps，数字等宽。
- 热力图：点彩/铅字网格，方格不发光；完成度通过瓶绿透明度表达，0 为纸色加灰线。
- 页边注：显示当前周、达标规则、存储状态等，不使用提示卡。
- 页脚：低调署名，链接占位为 `https://github.com/你的用户名`。

### 响应式与可访问性

- 所有可点击训练条目用 `button` 或具备 `role="button"`、`tabindex="0"` 和键盘事件。
- 保证 focus visible。
- 文本与背景对比度优先，不用浅字压浅底。
- `prefers-reduced-motion: reduce` 下禁用过渡动画。
- 手机端表格允许横向滚动，但主要内容不被挤压重叠。

### 有理由的美学风险

采用“页边批注系统”：桌面端在右侧页边展示当前周目标、达标规则和存储状态，像学术书页的边注；手机端这些边注会折到正文上方或下方。这比常规仪表盘卡片更有书籍感，也能让工具的状态信息不抢主内容。

## E. 自我批评与审美校正

初始方向里“旧纸色 + 衬线字体 + 暗红强调色”很容易滑进常见 AI 默认审美：奶油色背景、高对比衬线、赤陶/暗红按钮、很多圆角卡片。这个组合看似复古，其实很像模板化的文艺落地页。

因此本方案做了以下校正：

- 强调色从暗红改为瓶绿 `#174D3A`，避开赤陶和暗红带来的“默认复古咖啡馆”气质。
- 背景选冷象牙纸 `#F1EEE4`，不是偏黄奶油色。
- 结构不采用卡片堆叠，而采用学术编号、细线账本、页边批注、表格和目录式条目。
- 视觉重点从装饰转向文字排版、行距、线条、数字和留白。
- 热力图不做 GitHub 式亮色小方块，而做铅字印刷感网格。

最终希望页面像一本每天会被翻开的训练手册：克制、可靠、有一点旧学术刊物的严肃感，但不压抑。

## F. 设计复盘后的调整

对比参考稿后，原实现的问题不是功能，而是视觉锚点不够强：大留白、细线和侧栏批注让页面更像线框稿，少了“每天可用的手册”应有的密度与节奏。

调整方向：

- 保留瓶绿作为唯一强调色，不照搬参考稿的赤陶色，继续避开 AI 默认复古配色。
- 版心从宽幅学术页收紧为更像手册的中等宽度，顶部改为紧凑应用栏，减少落地页感。
- 今天页增加深色本周主题条，把“本周做什么”变成首屏视觉锚点。
- 今天页进一步显性化训练主线：小事 → 宏观变化 → 中间结构 → 耦合代价 → 英文判断 → 职场沟通。用户每天打开时先看到“不是来打卡，是练解释世界和与人共事”。
- 六个训练环节从长条账本改为 3×2 可点击账本格，提升触感和完成反馈。
- 中文字体补充 `Noto Serif SC`，避免中文落到不可控系统字体；英文标题仍用 `Newsreader`，数据与标签继续用 `IBM Plex Mono`。
- 页边批注保留，但桌面端压窄，移动端折叠为简洁状态区，不再抢主内容。

## G. 清理与打磨记录

本次只做清理与既有界面打磨，不改变 localStorage 数据模型与统计规则。

将要改动：

- 合并 `index.html` 中重复的两个 `<style>` 块与两套 `:root`，删除第一套已被覆盖的旧 token 和旧组件样式，只保留当前真实生效的视觉系统。
- 合并后确认最终 token 为：`--paper #F1F2ED`、`--ink #19231F`、`--muted #4F574F`、`--line #C9CBC0`、`--accent #174D3A`、`--measure 62ch`、`--serif` 含 `Noto Serif SC`。
- 给标题元素补 `text-wrap: balance`。
- 在“本周”表格中让某一行进入可编辑状态，直接编辑对应日期的六段完成状态、今日笔记与一句反思；仍复用既有 `day:YYYY-MM-DD` 数据，不增加新键。
- 本周反思摘要改为点按展开全文，触屏可用，不只依赖 `title`。
- 热力图增加“少 → 多”图例，使用同一瓶绿透明度。
- 增加 `@media print`，打印“本周”和“进度/回看”相关页面时隐藏导航与页边注，表格和账本元素降级为黑白纸面。

将删除：

- 第一套旧 `:root` 中不再生效的 `#F1EEE4 / #25211B / #5E574B / #9B9386 / 68ch` token。
- 第一套中被第二套覆盖的旧版 header、版心、训练条目、表格、热力图、按钮、移动端规则等重复样式。

## H. 结构化分析日志与 §6 回顾页计划

本次是增量升级：保留现有五页、路由、瓶绿学术风、localStorage 命名空间、统计规则和上一轮清理修复；只把“今日笔记”升级为可保存、可回看的结构化“每日分析日志”，并新增 `§6 回顾`。

### 新的每日记录结构

存储 key 仍保持 `ability-trainer:v1:day:YYYY-MM-DD`，不重新命名空间，不做破坏性迁移。每日记录在原结构上叠加 `analysis`：

```json
{
  "seg": {
    "input": false,
    "think": false,
    "output": false,
    "organize": false,
    "comms": false,
    "review": false
  },
  "analysis": {
    "subject": "",
    "origin": "",
    "structure": "",
    "conditions": "",
    "coupling": "",
    "conclusion": ""
  },
  "note": "",
  "reflection": "",
  "updatedAt": "2026-06-20T10:30:00.000Z"
}
```

`analysis` 六个字段对应“一件小事·深度分析”模板：

- `subject`：这件小事（要具体）。
- `origin`：来历：当年为什么出现。
- `structure`：通过什么中间结构。
- `conditions`：当年的条件现在还成立吗。
- `coupling`：耦合：真·牵一发 vs 草台班子。
- `conclusion`：一句话结论。

`note` 不删除，重新标为“今日产出 / 补充”，用于英文 150 词、自由输出或临时材料；`reflection` 仍是一句反思。达标逻辑继续只看六个训练环节完成数 `>= 4`，填写分析字段不会自动勾选任何环节。

### 向后兼容方案

- 读取旧日记录时统一走 `normalizeDay`：如果没有 `analysis`，补一个全空结构；如果部分字段缺失，只补缺失字段。
- 旧的 `note` 内容原样保留，显示在“今日产出 / 补充”中，不清洗、不拆分、不丢弃。
- `meta` key 仍为 `ability-trainer:v1:meta`；开始日期、周次、streak、热力图、达标率规则不变。
- JSON 导出 `version` 升到 `2`，导出内容包含每一天的 `analysis`。
- JSON 导入兼容 `version: 1` 或无版本文件：逐个 day 合并写入，冲突以导入文件为准；导入记录无 `analysis` 时自动补空结构，不报错。
- `localStorage` 不可用时继续使用内存降级；所有写操作保留 `try/catch`。

### §1 今天页调整

- 将原“今日笔记”单文本框替换为“今日分析”区块，视觉上沿用实验记录页/账本样式，不引入新颜色或卡片阴影。
- “今日分析”包含六个可输入多行字段，每个字段使用 small caps 标签 + 中文说明，输入时即时保存到 `day.analysis.*`。
- “今日分析”下方保留“今日产出 / 补充”自由文本框，绑定 `day.note`。
- “一句反思”保持原位置与行为，绑定 `day.reflection`。
- 本周页编辑过去某天时也应能编辑该天的分析、产出/补充与反思，避免 §1 和 §2 对同一日数据能力不一致。

### §6 回顾页信息架构

新增 hash 路由 `#archive`，导航编号为 `§6 回顾`。页面定位是“把每天的解释练习沉淀成可检索的思想账本”，不是新的统计仪表盘。

内容与交互：

- 顶部显示一个搜索框，过滤所有 `analysis` 字段内容；用于回看反复出现的“中间结构”、耦合判断和结论模式。
- 可加一个克制的周筛选控件，用于只看某一周或全部记录；若实现后空间显得拥挤，则优先保留搜索框。
- 主列表只展示 `analysis` 任一字段非空的日期，按日期倒序。
- 每条记录采用目录卡/账本行样式：日期、第几周、一句话结论作为醒目标题；若结论为空，用“未写结论的分析记录”作低调兜底。
- 点击或键盘激活某条记录后展开六字段全文；再次激活收起。触屏可用，不依赖 hover。
- 无分析记录时显示低调空状态，引导回到 `#today` 写第一条。

样式延续现有系统：衬线正文、small caps 标签、发丝线、瓶绿强调、纸面背景；不增加渐变、阴影、现代 SaaS 卡片或额外颜色。

### 模板页小改动

在“① 一件小事·深度分析”模板旁增加“用此模板填今天”按钮。点击后跳转到 `#today`，并聚焦“今日分析”的第一个字段 `subject`；按钮不预填内容，避免覆盖用户已经写下的当天分析。

## I. §7 习惯页计划

本次新增 `§7 习惯`，hash 路由为 `#habits`，纳入 § 编号导航。它是“数字极简 / 自律小挑战”的轻量清单页，不改 `§1 今天`，不把习惯写入今天页，也不改变现有六段训练、streak、达标率、热力图、回顾页或计划页的数据逻辑。

页面副标题固定为：精彩的世界，仍在屏幕之外继续发生。

### 数据模型

命名空间继续保持 `ability-trainer:v1:`，不新增应用级版本命名空间。

每天项写入当天记录：

```json
{
  "seg": {},
  "analysis": {},
  "note": "",
  "reflection": "",
  "habits": {
    "wake_now": true
  },
  "updatedAt": "2026-06-20T10:30:00.000Z"
}
```

规则：

- `day.habits = { habitId: true }` 只属于当天 `ability-trainer:v1:day:YYYY-MM-DD`。
- 新日期读取新 day，因此每天项自然重置。
- 读取旧 day 时如果没有 `habits`，在 `normalizeDay` 中补 `{}`；旧数据不报错、不迁移、不丢失。
- 勾选每天项时保存当天 day，但不影响 `seg`、达标、streak 或热力图。

每周项单独写入当前计划周号：

```json
{
  "learn_skill_hour": true
}
```

key 设计：

- `ability-trainer:v1:weekhabits:<当前计划周号>`
- `<当前计划周号>` 使用现有 `currentWeekNumber()` 计算出来的真实计划周号。
- 周号变化时读取另一个 key，因此每周项自然重置。
- 如果当前真实周次小于 1，也允许以该周号形成 key；界面可低调提示“计划尚未开始”，但不要阻止用户勾选。
- 每周习惯不参与计划页高亮、周统计或训练达标。

一次项单独写入：

```json
{
  "notif_off_24": "2026-06-20"
}
```

key 设计：

- `ability-trainer:v1:oncehabits`
- 值为 `{ habitId: "YYYY-MM-DD" }`，记录首次完成日期。
- 一次项勾选后永久保留并显示完成日期。
- 如果允许取消勾选，则只删除该 habitId；不影响其它一次项。

导出/导入：

- JSON 导出继续使用 `version: 2`，但增加：
  - `weekHabits`: `{ "<weekNumber>": { habitId: true } }`
  - `onceHabits`: `{ habitId: "YYYY-MM-DD" }`
  - 每日 `days[date]` 中包含 `habits`。
- 导入兼容旧文件：缺 `habits`、`weekHabits`、`onceHabits` 时按空处理，不报错。
- 导入仍按天合并 day 记录，冲突以导入文件为准；`weekHabits` 按周 key 合并，冲突以导入文件为准；`onceHabits` 按 habitId 合并，冲突以导入文件为准。
- `localStorage` 不可用时继续使用现有内存降级；所有写操作保留 `try/catch`。

### §7 信息架构

页面结构：

- `sectionHead(7, "习惯", "精彩的世界，仍在屏幕之外继续发生。")`
- 三组朴素清单：`每天 Daily`、`每周 Weekly`、`一次 One-time`。
- 每组只显示条目，不加 streak、热力图、进度百分比或额外统计。
- 每个条目是一行可勾选账本条目：
  - 左侧方形 ballot box。
  - 右侧第一行加粗“行动”。
  - 第二行 muted 小字说明；若原文说明为空，则不显示空行。
  - small caps 组标签和条目 id 可以低调显示，用于保持账本索引感。
- 键盘可达：条目使用 `button` 或带明确 `type="button"` 的控件，支持 focus visible；`aria-pressed` 或等价状态要准确。
- 视觉沿用现有学术账本风格：发丝线、瓶绿强调、纸面背景、方形 ballot box、small caps 标签；不引入新颜色、渐变、阴影或现代卡片堆叠。

三组行为：

- 每天：读取今天 `day.habits`，点按即时保存到今天记录；明天自然显示为空。
- 每周：读取当前计划周号对应的 `weekhabits` key，点按即时保存；下周自然显示为空。
- 一次：读取 `oncehabits`，点按后写入今天日期并显示“完成于 YYYY-MM-DD”；再次点按可取消，删除该项完成日期。

条目清单：

每天 `daily`：

- `wake_now`｜闹钟响起立刻起床｜和自己讨价还价，只会毁掉全新的一天。
- `hard_first`｜先花30分钟做一件困难的事，再娱乐｜先生产，后享受。
- `train_anyway`｜哪怕不想动，也完成今天的训练｜真正的纪律，在这种时刻诞生。
- `focus_one_hour`｜专注一小时只做一件事，不切标签页或 App｜
- `write_learned`｜写下今天学到的东西｜深度思考和认真写作，几乎是同一件事。
- `meal_no_screen`｜认真吃一顿饭，面前不放任何屏幕｜品尝食物，而不是刷算法。
- `eat_slow`｜吃饭时故意放慢速度｜匆忙，早已成为一种隐形瘾。
- `eye_contact`｜别人说话时，专注看着对方的眼睛｜真正的注意力，是最珍贵的礼物。
- `phone_out_bedroom`｜今晚把手机放在卧室外面｜睡眠质量会明显提升。
- `sit_still_10`｜静静坐10分钟，什么也不做｜听起来简单，做起来却很难。

每周 `weekly`：

- `morning_no_phone`｜完整度过一个早晨，完全不碰手机｜重新找回独立思考的清晰感。
- `afternoon_no_social`｜用一个下午远离社交媒体｜你会发现自己找回了多少时间。
- `walk_no_audio`｜步行30分钟，不听音乐或播客｜真正的沉默，是最强的复位工具。
- `wander_walk`｜漫无目的地散步半小时｜有时候，刻意迷失正是最好的前进。
- `read_30`｜阅读一本书的30页｜专注力会慢慢苏醒。
- `learn_skill_hour`｜花一小时学习一项全新技能｜大脑最渴望真实的挑战。
- `documentary`｜看一部纪录片，而不是刷剧集｜优质娱乐与深度学习可以并存。
- `deep_dive`｜深入研究一个长期好奇的话题｜追随好奇心，而不是跟着算法。
- `call_someone`｜打电话给某人，而不是只发消息｜人声里有文字传不出的温度。
- `watch_sunset`｜完整看一次日落，不拿手机｜并非所有美好都要拍成内容。
- `sleep_early`｜比平时早一小时睡觉｜长期的疲惫，是最隐秘的盗贼。

一次 `one-time`：

- `notif_off_24`｜关闭所有通知，持续24小时｜多数所谓紧急，只是廉价干扰。
- `finish_procrastinated`｜完成一项拖延了好几周的任务｜停止逃避，能量自然回来。
- `one_idea_for_life`｜记住一个能陪伴一生的深刻想法｜最强的工具，往往浓缩成一句话。
- `real_memories_day`｜花一整天积累真实回忆，而不是制造内容｜生命结束时，你真正记住的只有那些时刻。

## J. §8 日程页计划

本次新增 `§8 日程`，hash 路由为 `#schedule`，纳入 § 编号导航。它是“每日照着做”的当日 run-sheet，不改 `§1 今天` 或 `§7 习惯` 的数据逻辑，不把日程完成情况写入训练达标、streak、热力图或习惯统计。

默认打开页改动：

- 当前 app 首次进入时，如果没有 hash，从 `#today` 改为 `#schedule`。
- 用户主动打开其它 hash 时尊重当前 hash，不强制跳转。

页面副标题固定为：任何一天打开它，照着做。

### 数据模型

命名空间继续保持 `ability-trainer:v1:`。

当天 run-sheet 勾选写入当天记录：

```json
{
  "seg": {},
  "analysis": {},
  "note": "",
  "reflection": "",
  "habits": {},
  "runsheet": {
    "jobcore": true
  },
  "updatedAt": "2026-06-21T10:30:00.000Z"
}
```

规则：

- `day.runsheet = { blockId: true }` 只属于当天 `ability-trainer:v1:day:YYYY-MM-DD`。
- 新日期读取新 day，因此 run-sheet 勾选自然按日期重置。
- 读取旧 day 时如果没有 `runsheet`，在 `normalizeDay` 中补 `{}`；旧数据不报错、不迁移、不丢失。
- 勾选 run-sheet 只保存当天 day，不影响 `seg`、`analysis`、`habits`、训练达标、streak、热力图或回顾页。
- 当天关键信号只看 `runsheet.jobcore` 是否完成，不计算总完成率，不做 streak。

计算机方向写入 `meta`：

```json
{
  "start": "2026-06-20",
  "computerTrack": "portfolio"
}
```

规则：

- `meta.computerTrack` 只允许 `"portfolio"` 或 `"interview"`。
- 旧 `meta` 缺字段时默认 `"portfolio"`。
- 切换方向后保存到 `ability-trainer:v1:meta`，并立即影响 `cs` 块显示内容。
- `meta.start` 的既有逻辑不变。

轮换内容：

- 雅思块和弹性块按浏览器本地日期的星期计算，不存储。
- `cs` 块按 `meta.computerTrack` 计算，不写入 day。

导出/导入：

- JSON 导出继续使用 `version: 2`，但每日 `days[date]` 中包含 `runsheet`，`meta` 中包含 `computerTrack`。
- 导入兼容旧文件：缺 `runsheet` 或 `computerTrack` 时按空/默认处理，不报错。
- 导入仍按天合并 day 记录，冲突以导入文件为准；`meta.computerTrack` 合法时更新，不合法则忽略并保留现有或默认值。
- `localStorage` 不可用时继续使用现有内存降级；所有写操作保留 `try/catch`。

### §8 信息架构

页面结构：

- `sectionHead(8, "日程", "任何一天打开它，照着做。")`
- 顶部“铁律”提醒条，只读，不是打卡项。
- 计算机方向设置区：作品集向 / 面试向，默认作品集向；旁边显示原文决策规则提示。
- 当天关键信号：只显示求职核心块是否完成，例如“求职核心块：未完成 / 已完成”，不显示总完成率。
- 主体“今日骨架”：有线时间表/账本，每个时间块一行，左侧 ballot box 可勾选，时间使用等宽字体。
- 时间只是建议，页面不做计时器、不做通知、不做自动提醒。

视觉规则：

- 沿用现有学术账本风格：发丝线、方形 ballot box、small caps 标签、瓶绿强调、纸面背景。
- 不引入新颜色、渐变、阴影或现代卡片堆叠。
- `jobcore` 求职核心块标为『非协商』，使用瓶绿左边线 + 小标签等克制强调。
- 顶部铁律中“签证 + 投递 = 唯一非协商项...”这一条用瓶绿强调并最显眼。

### 铁律

顶部只读提醒，原文：

1. 手机不在床头——昨晚就放客厅充电。睁眼第一件事是起床，不是碰手机。
2. 签证 + 投递 = 唯一非协商项，排在所有事前面，直到 Graduate Route 交上去为止。其余全是加分。【强调】
3. 一天崩一块，从下一块接上，别因为崩了一块就把整天作废。
4. 起床时间最不重要：7:30 起不来就 8:00，后面整体顺延，别因为没早起就废掉一天。

### 今日骨架

固定 blockId 与建议时间：

- `wake`｜7:30｜起床，手机在客厅
- `cardio`｜7:30–8:30｜跑步/有氧 + 冲澡（今天第一个杠杆；走跑交替起步，慢慢加）
- `breakfast`｜8:30–9:00｜早餐 + 换上出门的衣服
- `jobcore`｜9:00–10:30｜【非协商·求职核心块】手机扔另一房间：签证往前推 + 投 2–3 个岗 → 做完今天就算赢
- `break1`｜10:30–10:45｜休息，光明正大刷手机
- `ielts`｜10:45–12:15｜雅思块（内容按星期）
- `lunch`｜12:15–13:15｜午饭 + 真休息
- `cs`｜13:15–14:45｜计算机块（内容按所选方向）
- `break2`｜14:45–15:00｜休息
- `flex`｜15:00–17:00｜弹性块（内容按星期）
- `landing`｜17:00 之后｜软着陆，吃饭，真下班
- `night`｜睡前 2 分钟｜定明天第一个动作，手机放回客厅

### 雅思块周轮换

按星期填入 `ielts` 块：

- 周一 / 周四｜写作：Task 2 结构 + Task 1 图表（你的主要丢分项）
- 周二｜口语 + 听力
- 周三｜阅读 + 词汇/搭配（Anki）
- 周五｜一套限时单项
- 周六 / 周日｜完整模考 + 复盘（周末做一次即可）

`ielts` 块额外小字提示原文：

第一周先做一套完整限时模考定位；写作最难自评，写完找人按评分标准逐条过。

### 计算机方向

`cs` 块按 `meta.computerTrack` 填：

- `portfolio` 作品集向｜SQL / 云 / 数据管道，或把两个 ML 项目做深 → 直接喂求职
- `interview` 面试向｜LeetCode 算法/数据结构 → 技术面准备

设置区决策规则提示原文：

还没怎么进面试，先走作品集向；开始接技术面，再转面试向。这 3 个月雅思优先，计算机维持轻量；雅思考完再加重。

### 弹性块周轮换

按星期填入 `flex` 块：

- 周二 / 周四 / 周六｜出门扫街：餐饮/零售/中介，递简历，问能不能跟经理聊两句
- 其余日｜更多投递，或继续作品集/计算机
